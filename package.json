{
  "name": "node-ts-jest-webpack-boiler",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "lint": "tslint --fix -c tslint.json 'src/**/*.{ts,tsx,js,json,css,md}'",
    "format": "prettier --write 'src/**/*.{ts,tsx,js,json,css,md}'",
    "type-check": "tsc --skipLibCheck",
    "test": "jest --passWithNoTests",
    "pre-build": "run-s clean lint format type-check test",
    "build": "yarn pre-build && webpack",
    "build:watch": "yarn pre-build && webpack --watch",
    "run:node": "node ./dist/index.js",
    "run:nodemon": "nodemon ./dist/index.js",
    "start": "run-s build run:node",
    "start:watch": "run-p build:watch run:nodemon",
    "pre-commit": "yarn build"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn pre-commit && lint-staged"
    }
  },
  "lint-staged": {
    "src/**/*.{ts,tsx,js,json,css,md}": [
      "tslint --fix",
      "prettier --write",
      "git add"
    ]
  },
  "devDependencies": {
    "@babel/core": "^7.4.0",
    "@babel/plugin-proposal-class-properties": "^7.4.0",
    "@babel/plugin-proposal-object-rest-spread": "^7.4.0",
    "@babel/preset-typescript": "^7.3.3",
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.0",
    "babel-loader": "^8.0.5",
    "husky": "^1.3.1",
    "jest": "^24.6.0",
    "lint-staged": "^8.1.5",
    "nodemon": "^1.18.10",
    "npm-run-all": "^4.1.5",
    "prettier": "1.16.4",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.1",
    "tslint": "^5.15.0",
    "typescript": "^3.4.1",
    "webpack": "^4.29.6",
    "webpack-cli": "^3.3.0"
  }
}
